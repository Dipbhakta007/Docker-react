sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t Dipbhakta007/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Dipbhakta007/docker-react npm run test